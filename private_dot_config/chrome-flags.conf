# Read by Arch's google-chrome-stable wrapper for desktop and Win+B launches.
# Suppress the debugger-extension notification; restart Chrome to take effect.
# Rollback: remove the flag below, re-add with chezmoi, and restart Chrome.
--silent-debugger-extension-api
