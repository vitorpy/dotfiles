# Prefer user-installed wrapper scripts/binaries over system ones for
# anything launched from the graphical session (app launchers, docks, etc.),
# not just interactive shells.
PATH=%h/.local/bin:${PATH}
