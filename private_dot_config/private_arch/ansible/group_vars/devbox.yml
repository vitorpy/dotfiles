---
arch_devbox_system_services_extra:
  - cronie
  - et

arch_devbox_firewall_allowed_tcp_ports_extra:
  - 2022

arch_system_services: "{{ arch_server_system_services + arch_devbox_system_services_extra }}"
arch_firewall_allowed_tcp_ports: "{{ (arch_server_firewall_allowed_tcp_ports + arch_devbox_firewall_allowed_tcp_ports_extra) | unique }}"

arch_pacman_packages: "{{ arch_server_pacman_packages + arch_pacman_packages_server_devbox }}"
arch_aur_packages: "{{ arch_server_aur_packages + arch_aur_packages_server }}"
