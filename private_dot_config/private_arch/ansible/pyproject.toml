[project]
name = "arch-ansible"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests/python"]
pythonpath = ["roles/services/files/media-sorter"]
