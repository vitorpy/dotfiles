# Managed by Ansible. New processes receive this limit after the next complete
# system and user manager activation boundary.
[Manager]
DefaultLimitNOFILE={{ arch_default_limit_nofile_soft }}:{{ arch_default_limit_nofile_hard }}
