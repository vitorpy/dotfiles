diff --git a/internal/protocol/dht/server/socket_unix.go b/internal/protocol/dht/server/socket_unix.go
index 4f9eec2..54ae229 100644
--- a/internal/protocol/dht/server/socket_unix.go
+++ b/internal/protocol/dht/server/socket_unix.go
@@ -63 +63,3 @@ func addrPortToSockaddr(addr netip.AddrPort) (unix.Sockaddr, error) {
-	if addr.Addr().Is4() {
+	ip := addr.Addr().Unmap()
+
+	if ip.Is4() {
@@ -65 +67 @@ func addrPortToSockaddr(addr netip.AddrPort) (unix.Sockaddr, error) {
-			Addr: addr.Addr().As4(),
+			Addr: ip.As4(),
@@ -69 +71 @@ func addrPortToSockaddr(addr netip.AddrPort) (unix.Sockaddr, error) {
-	if addr.Addr().Is6() {
+	if ip.Is6() {
@@ -71 +73 @@ func addrPortToSockaddr(addr netip.AddrPort) (unix.Sockaddr, error) {
-			Addr: addr.Addr().As16(),
+			Addr: ip.As16(),
diff --git a/internal/protocol/dht/server/socket_unix_test.go b/internal/protocol/dht/server/socket_unix_test.go
new file mode 100644
index 0000000..ef10962
--- /dev/null
+++ b/internal/protocol/dht/server/socket_unix_test.go
@@ -0,0 +1,50 @@
+//go:build !windows
+
+package server
+
+import (
+	"net/netip"
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+	"golang.org/x/sys/unix"
+)
+
+func TestAddrPortToSockaddr(t *testing.T) {
+	t.Parallel()
+
+	tests := []struct {
+		name string
+		addr netip.AddrPort
+		want any
+	}{
+		{
+			name: "IPv4-mapped IPv6 is treated as IPv4",
+			addr: netip.AddrPortFrom(
+				netip.MustParseAddr("::ffff:1.2.3.4"),
+				6881,
+			),
+			want: (*unix.SockaddrInet4)(nil),
+		},
+		{
+			name: "pure IPv4",
+			addr: netip.AddrPortFrom(netip.MustParseAddr("1.2.3.4"), 6881),
+			want: (*unix.SockaddrInet4)(nil),
+		},
+		{
+			name: "pure IPv6",
+			addr: netip.AddrPortFrom(netip.MustParseAddr("2001:db8::1"), 6881),
+			want: (*unix.SockaddrInet6)(nil),
+		},
+	}
+
+	for _, tt := range tests {
+		t.Run(tt.name, func(t *testing.T) {
+			t.Parallel()
+
+			got, err := addrPortToSockaddr(tt.addr)
+			assert.NoError(t, err)
+			assert.IsType(t, tt.want, got)
+		})
+	}
+}
